import React from 'react';

const NavMenu = (props) => {
  const styles = {
    justifySelf: 'end'
  };

  return (
    <div style={styles}>
      <i class="fas fa-bars"></i>
    </div>
  );
};

export default NavMenu;